<div class="w-full">
  <div class="mb-8 lg:hidden">
    <div class="flex items-center gap-3">
      <div class="flex h-11 w-11 items-center justify-center rounded-xl bg-indigo-600 text-sm font-bold text-white shadow-sm">
        CF
      </div>

      <div>
        <p class="text-lg font-bold tracking-tight text-gray-900">
          ClientFlow
        </p>

        <p class="text-sm text-gray-500">
          Customer relationship management
        </p>
      </div>
    </div>
  </div>

  <div>
    <p class="text-sm font-semibold uppercase tracking-wide text-indigo-600">
      Welcome back
    </p>

    <h1 class="mt-2 text-3xl font-bold tracking-tight text-gray-900">
      Sign in to your account
    </h1>

    <p class="mt-3 text-sm leading-6 text-gray-600">
      Access your clients, leads, deals, tasks, and reports.
    </p>
  </div>

  <%= form_for(
        resource,
        as: resource_name,
        url: session_path(resource_name),
        html: {
          class: "mt-8 space-y-5"
        }
      ) do |form| %>
    <div>
      <%= form.label :email,
                     class: "block text-sm font-semibold text-gray-700" %>

      <%= form.email_field :email,
                           autofocus: true,
                           autocomplete: "email",
                           placeholder: "you@example.com",
                           class: "mt-2 block w-full rounded-lg border border-gray-300 bg-white px-4 py-3 text-sm text-gray-900 shadow-sm outline-none transition placeholder:text-gray-400 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" %>
    </div>

    <div>
      <div class="flex items-center justify-between gap-4">
        <%= form.label :password,
                       class: "block text-sm font-semibold text-gray-700" %>

        <% if devise_mapping.recoverable? %>
          <%= link_to "Forgot password?",
                      new_password_path(resource_name),
                      class: "text-sm font-semibold text-indigo-600 transition hover:text-indigo-800" %>
        <% end %>
      </div>

      <%= form.password_field :password,
                              autocomplete: "current-password",
                              placeholder: "Enter your password",
                              class: "mt-2 block w-full rounded-lg border border-gray-300 bg-white px-4 py-3 text-sm text-gray-900 shadow-sm outline-none transition placeholder:text-gray-400 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="flex items-center gap-3">
        <%= form.check_box :remember_me,
                           class: "h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-500" %>

        <%= form.label :remember_me,
                       "Keep me signed in",
                       class: "text-sm text-gray-600" %>
      </div>
    <% end %>

    <%= form.submit "Sign in",
                    class: "w-full cursor-pointer rounded-lg bg-indigo-600 px-5 py-3 text-sm font-semibold text-white shadow-sm transition hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" %>
  <% end %>

  <% if devise_mapping.registerable? %>
    <p class="mt-8 text-center text-sm text-gray-600">
      Don’t have an account?

      <%= link_to "Create one",
                  new_registration_path(resource_name),
                  class: "font-semibold text-indigo-600 transition hover:text-indigo-800" %>
    </p>
  <% end %>
</div>
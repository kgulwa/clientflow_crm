<div class="space-y-8">
  <section class="flex flex-col justify-between gap-4 sm:flex-row sm:items-center">
    <div>
      <p class="text-sm font-semibold uppercase tracking-[0.14em] text-stone-500">
        Dashboard
      </p>

      <h1 class="mt-1 text-3xl font-bold tracking-tight text-slate-900">
        Welcome back, <%= current_user.first_name %>!
      </h1>

      <p class="mt-2 text-slate-600">
        Here is an overview of your clients, leads, and upcoming work.
      </p>
    </div>

    <div class="flex flex-col gap-3 sm:flex-row">
      <%= link_to "Add lead",
                  new_lead_path,
                  class: "rounded-xl border border-slate-300 bg-white/60 px-5 py-3 text-center text-sm font-semibold text-slate-700 transition hover:border-slate-400 hover:bg-white hover:text-slate-900" %>

      <%= link_to "Add client",
                  new_client_path,
                  class: "rounded-xl bg-slate-800 px-5 py-3 text-center text-sm font-semibold text-white shadow-sm transition hover:bg-slate-900" %>
    </div>
  </section>

  <section class="grid gap-5 sm:grid-cols-2 xl:grid-cols-4">
    <article class="rounded-2xl border border-white/70 bg-white/65 p-6 shadow-sm backdrop-blur">
      <div class="flex items-start justify-between">
        <div>
          <p class="text-sm font-medium text-slate-500">
            Total clients
          </p>

          <p class="mt-3 text-3xl font-bold text-slate-900">
            <%= @total_clients %>
          </p>
        </div>

        <div class="rounded-xl bg-slate-100 px-3 py-2 text-lg font-semibold text-slate-700">
          C
        </div>
      </div>

      <p class="mt-4 text-sm text-slate-500">
        All clients in your CRM.
      </p>
    </article>

    <article class="rounded-2xl border border-white/70 bg-white/65 p-6 shadow-sm backdrop-blur">
      <div class="flex items-start justify-between">
        <div>
          <p class="text-sm font-medium text-slate-500">
            Active leads
          </p>

          <p class="mt-3 text-3xl font-bold text-slate-900">
            <%= @lead_clients %>
          </p>
        </div>

        <div class="rounded-xl bg-[#f5ead7] px-3 py-2 text-lg font-semibold text-[#9b661f]">
          L
        </div>
      </div>

      <p class="mt-4 text-sm text-slate-500">
        Potential clients already tracked as client records.
      </p>
    </article>

    <article class="rounded-2xl border border-white/70 bg-white/65 p-6 shadow-sm backdrop-blur">
      <div class="flex items-start justify-between">
        <div>
          <p class="text-sm font-medium text-slate-500">
            Active clients
          </p>

          <p class="mt-3 text-3xl font-bold text-slate-900">
            <%= @active_clients %>
          </p>
        </div>

        <div class="rounded-xl bg-[#e5eee8] px-3 py-2 text-lg font-semibold text-[#3e7157]">
          A
        </div>
      </div>

      <p class="mt-4 text-sm text-slate-500">
        Current active client relationships.
      </p>
    </article>

    <article class="rounded-2xl border border-white/70 bg-white/65 p-6 shadow-sm backdrop-blur">
      <div class="flex items-start justify-between">
        <div>
          <p class="text-sm font-medium text-slate-500">
            Pending tasks
          </p>

          <p class="mt-3 text-3xl font-bold text-slate-900">
            <%= @pending_tasks_count %>
          </p>
        </div>

        <div class="rounded-xl bg-[#ecebea] px-3 py-2 text-lg font-semibold text-[#55616c]">
          T
        </div>
      </div>

      <p class="mt-4 text-sm text-slate-500">
        <%= pluralize(@overdue_tasks_count, "overdue task") %>.
      </p>
    </article>
  </section>

  <section class="grid gap-6 xl:grid-cols-3">
    <div class="rounded-2xl border border-white/70 bg-white/65 shadow-sm backdrop-blur xl:col-span-2">
      <div class="flex items-center justify-between border-b border-stone-200 px-6 py-5">
        <div>
          <h2 class="text-lg font-semibold text-slate-900">
            Recent leads
          </h2>

          <p class="mt-1 text-sm text-slate-500">
            Your five most recently added sales opportunities.
          </p>
        </div>

        <%= link_to "View all leads",
                    leads_path,
                    class: "text-sm font-semibold text-slate-700 transition hover:text-slate-950" %>
      </div>

      <% if @recent_leads.any? %>
        <div class="divide-y divide-stone-200">
          <% @recent_leads.each do |lead| %>
            <% status_classes = case lead.status
                                when "qualified"
                                  "bg-[#e5eee8] text-[#3e7157]"
                                when "contacted"
                                  "bg-[#e8edf3] text-[#3e5f7d]"
                                when "lost"
                                  "bg-[#f3e3e0] text-[#9d443d]"
                                when "converted"
                                  "bg-[#ecebea] text-[#55616c]"
                                else
                                  "bg-[#f5ead7] text-[#9b661f]"
                                end %>

            <article class="flex flex-col justify-between gap-4 px-6 py-5 sm:flex-row sm:items-center">
              <div>
                <div class="flex flex-wrap items-center gap-2">
                  <h3 class="font-semibold text-slate-900">
                    <%= lead.full_name %>
                  </h3>

                  <span class="inline-flex rounded-full px-2.5 py-1 text-xs font-semibold <%= status_classes %>">
                    <%= lead.display_status %>
                  </span>
                </div>

                <p class="mt-2 text-sm text-slate-500">
                  <%= lead.company_name.presence || "No company added" %>
                  ·
                  <%= lead.source == "linkedin" ? "LinkedIn" : lead.source.titleize %>
                </p>
              </div>

              <%= link_to "View lead",
                          lead_path(lead),
                          class: "text-sm font-semibold text-slate-700 transition hover:text-slate-950" %>
            </article>
          <% end %>
        </div>
      <% else %>
        <div class="flex min-h-64 flex-col items-center justify-center px-6 py-12 text-center">
          <div class="flex h-12 w-12 items-center justify-center rounded-full bg-[#f5ead7] text-xl font-semibold text-[#9b661f]">
            L
          </div>

          <h3 class="mt-4 font-semibold text-slate-900">
            No leads yet
          </h3>

          <p class="mt-2 max-w-sm text-sm text-slate-500">
            Add your first lead to begin tracking new sales opportunities.
          </p>

          <%= link_to "Add your first lead",
                      new_lead_path,
                      class: "mt-5 text-sm font-semibold text-slate-700 transition hover:text-slate-950" %>
        </div>
      <% end %>
    </div>

    <div class="rounded-2xl border border-white/70 bg-white/65 shadow-sm backdrop-blur">
      <div class="border-b border-stone-200 px-6 py-5">
        <h2 class="text-lg font-semibold text-slate-900">
          Lead summary
        </h2>

        <p class="mt-1 text-sm text-slate-500">
          A quick look at your current lead pipeline.
        </p>
      </div>

      <div class="space-y-4 p-6">
        <div class="flex items-center justify-between rounded-xl bg-slate-100/80 px-4 py-3">
          <span class="text-sm font-medium text-slate-600">
            Total leads
          </span>

          <span class="text-lg font-bold text-slate-900">
            <%= @total_leads %>
          </span>
        </div>

        <div class="flex items-center justify-between rounded-xl bg-[#f5ead7] px-4 py-3">
          <span class="text-sm font-medium text-[#9b661f]">
            New
          </span>

          <span class="text-lg font-bold text-[#9b661f]">
            <%= @new_leads %>
          </span>
        </div>

        <div class="flex items-center justify-between rounded-xl bg-[#e5eee8] px-4 py-3">
          <span class="text-sm font-medium text-[#3e7157]">
            Qualified
          </span>

          <span class="text-lg font-bold text-[#3e7157]">
            <%= @qualified_leads %>
          </span>
        </div>

        <div class="flex items-center justify-between rounded-xl bg-[#ecebea] px-4 py-3">
          <span class="text-sm font-medium text-[#55616c]">
            Converted
          </span>

          <span class="text-lg font-bold text-[#55616c]">
            <%= @converted_leads %>
          </span>
        </div>

        <div class="grid gap-3 pt-2">
          <%= link_to "Add a new lead",
                      new_lead_path,
                      class: "block w-full rounded-xl bg-slate-800 px-4 py-3 text-center text-sm font-semibold text-white transition hover:bg-slate-900" %>

          <%= link_to "View all leads",
                      leads_path,
                      class: "block w-full rounded-xl border border-stone-300 bg-white/50 px-4 py-3 text-center text-sm font-semibold text-slate-700 transition hover:bg-white hover:text-slate-900" %>
        </div>
      </div>
    </div>
  </section>

  <section class="grid gap-6 xl:grid-cols-3">
    <div class="rounded-2xl border border-white/70 bg-white/65 shadow-sm backdrop-blur xl:col-span-2">
      <div class="flex items-center justify-between border-b border-stone-200 px-6 py-5">
        <div>
          <h2 class="text-lg font-semibold text-slate-900">
            Upcoming tasks
          </h2>

          <p class="mt-1 text-sm text-slate-500">
            Your next client follow-ups and deadlines.
          </p>
        </div>

        <%= link_to "View clients",
                    clients_path,
                    class: "text-sm font-semibold text-slate-700 transition hover:text-slate-950" %>
      </div>

      <% if @upcoming_tasks.any? %>
        <div class="divide-y divide-stone-200">
          <% @upcoming_tasks.each do |task| %>
            <% overdue = task.due_date < Date.current %>

            <% priority_classes = case task.priority
                                  when "high"
                                    "bg-[#f3e3e0] text-[#9d443d]"
                                  when "low"
                                    "bg-[#e5eee8] text-[#3e7157]"
                                  else
                                    "bg-[#f5ead7] text-[#9b661f]"
                                  end %>

            <article class="flex flex-col justify-between gap-4 px-6 py-5 sm:flex-row sm:items-center">
              <div>
                <div class="flex flex-wrap items-center gap-2">
                  <h3 class="font-semibold text-slate-900">
                    <%= task.title %>
                  </h3>

                  <span class="inline-flex rounded-full px-2.5 py-1 text-xs font-semibold <%= priority_classes %>">
                    <%= task.priority.titleize %> priority
                  </span>

                  <% if overdue %>
                    <span class="inline-flex rounded-full bg-[#f3e3e0] px-2.5 py-1 text-xs font-semibold text-[#9d443d]">
                      Overdue
                    </span>
                  <% end %>
                </div>

                <p class="mt-2 text-sm text-slate-500">
                  <%= task.client.full_name %>
                  ·
                  Due <%= task.due_date.strftime("%B %-d, %Y") %>
                </p>
              </div>

              <%= link_to "View client",
                          client_path(task.client),
                          class: "text-sm font-semibold text-slate-700 transition hover:text-slate-950" %>
            </article>
          <% end %>
        </div>
      <% else %>
        <div class="flex min-h-64 flex-col items-center justify-center px-6 py-12 text-center">
          <div class="flex h-12 w-12 items-center justify-center rounded-full bg-slate-100 text-xl font-semibold text-slate-500">
            ✓
          </div>

          <h3 class="mt-4 font-semibold text-slate-900">
            You are all caught up
          </h3>

          <p class="mt-2 max-w-sm text-sm text-slate-500">
            Pending and in-progress client tasks will appear here.
          </p>
        </div>
      <% end %>
    </div>

    <div class="rounded-2xl border border-white/70 bg-white/65 shadow-sm backdrop-blur">
      <div class="border-b border-stone-200 px-6 py-5">
        <h2 class="text-lg font-semibold text-slate-900">
          Quick actions
        </h2>

        <p class="mt-1 text-sm text-slate-500">
          Common actions for managing your CRM.
        </p>
      </div>

      <div class="space-y-3 p-6">
        <% quick_action_classes = "block w-full rounded-xl border border-stone-200 bg-white/40 px-4 py-3 text-left text-sm font-medium text-slate-700 transition hover:border-slate-300 hover:bg-white hover:text-slate-950" %>

        <%= link_to "Add a new lead",
                    new_lead_path,
                    class: quick_action_classes %>

        <%= link_to "View all leads",
                    leads_path,
                    class: quick_action_classes %>

        <%= link_to "Add a new client",
                    new_client_path,
                    class: quick_action_classes %>

        <%= link_to "View all clients",
                    clients_path,
                    class: quick_action_classes %>

        <%= link_to "Manage client tasks",
                    clients_path,
                    class: quick_action_classes %>
      </div>
    </div>
  </section>
</div>
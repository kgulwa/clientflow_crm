<%= turbo_stream.replace "contacts_section" do %>
  <%= render "clients/contacts_section",
             client: @client,
             contact: @client.contacts.new,
             contacts: @contacts,
             editing_contact: nil %>
<% end %>